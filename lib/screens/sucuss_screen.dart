import 'package:flutter/material.dart';

class SucussScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sucuss Screen'),
      ),
      body: Center(
        child: Text('Succussfull'),
      ),
    );
  }
}
